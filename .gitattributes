# Normalize line endings to LF in the repository so CI/Cloudflare builds
# (and prettier) stay consistent across Windows/macOS/Linux.
* text=auto eol=lf

# Binary assets — never normalize
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.svg text
*.woff binary
*.woff2 binary
*.ttf binary
